define 'kryptnostic.caching-provider.session-storage', [
  'require'
  'kryptnostic.logger'
], (require) ->

  Logger = require 'kryptnostic.logger'

  log = Logger.get('SessionStorageCachingProvider')

  #
  # Author: dbailey
  #
  class SessionStorageCachingProvider


  return SessionStorageCachingProvider
